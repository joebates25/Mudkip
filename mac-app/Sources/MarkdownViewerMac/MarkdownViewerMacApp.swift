import SwiftUI

@main
struct MarkdownViewerMacApp: App {
    var body: some Scene {
        WindowGroup("Markdown Viewer") {
            ContentView()
        }
        .defaultSize(width: 1100, height: 760)
    }
}
